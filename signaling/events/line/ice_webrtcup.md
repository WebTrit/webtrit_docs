# `ice_webrtcup` line event

Notifying that `ICE` and `DTLS` succeeded, and so the WebRTC server correctly established a PeerConnection with the user/application.

## Additional request properties

None.

## Example

```json
{
  "event": "ice_webrtcup",
  "line": 0
}
```
